
```
【任务背景】
我有一个基于 Gradio 的膳食管理应用，代码见附件 foodManager.py。
目前是单用户模式（JSON 文件存储在 user_data/ 下），现在需要升级为**多用户注册/登录系统**，具体要求如下：

---

【1. 登录与注册页面（漂亮独立界面）】
- 在应用启动时，首先显示一个**自定义登录/注册页面**（而不是 Gradio 自带的弹窗）。
- 页面风格要与现有“膳康管家”的**绿白医疗健康风**一致（主色 #059669，圆角，柔和阴影）。
- 页面包含：
  - 一个 **登录表单**（用户名 + 密码 + 登录按钮）
  - 一个 **切换到注册表单** 的链接或按钮（点击后显示注册表单，包含用户名 + 密码 + 确认密码 + 注册按钮）
- 登录/注册成功后，自动切换到主应用界面（即现有的全部功能标签页）。
- 在主界面右上角或侧边栏显示当前登录用户名，并提供 **“退出登录”** 按钮（点击后回到登录页）。

---

【2. 用户管理（账号存储与密码安全）】
- 使用 `user_data/users.json` 存储所有注册用户信息，格式如下：
  ```json
  {
    "用户名": {
      "password_hash": "加盐的哈希值（使用 hashlib.sha256 + 随机盐）",
      "salt": "随机盐值",
      "created_at": "注册时间"
    }
  }
  ```
- 注册时需校验用户名是否已存在，密码长度至少 6 位。
- 登录时验证密码哈希是否匹配。

---

【3. 数据隔离（每个用户独立目录）】
- 重构现有 `DietManager` 类，使其在初始化时接收一个 `username` 参数。
- 所有数据读写路径改为：`user_data/{username}/records.json` 和 `user_data/{username}/profile.json`。
- 每个用户首次登录（或注册成功）时，自动创建其专属子目录。
- 不再使用根目录下的旧 `records.json` 和 `profile.json`（即旧数据作废，可忽略或备份后删除，但不要影响新系统运行）。

---

【4. 会话管理（如何在 Gradio 中保持登录状态）】
- 使用 `gr.State` 存储当前登录用户名。
- 利用 Gradio 的 `visible` 属性或 `gr.Blocks` 的条件渲染，实现“登录页”与“主应用”两个区块的切换（登录成功后隐藏登录区块，显示主应用区块；退出登录则相反）。
- 所有主界面的功能（识别、记录、查询、导出、AI 聊天等）都必须基于 `gr.State` 中的用户名来调用对应的 `DietManager` 实例。
- 需确保未登录状态下，主界面的任何功能不可见或不可操作。

---

【5. 对现有代码的具体改动要求】
- 尽量保留现有所有功能模块（识别、记录、统计、导出、AI 聊天、周趋势等）的逻辑不变，仅将单例的 `diet_manager` 改为动态获取（根据当前用户创建或复用）。
- 新增一个用户管理工具函数模块（如 `user_manager.py` 或直接在 `foodManager.py` 中增加相关函数）。
- 提供完整的代码修改方案，并注明每个修改点。
- 启动方式保持不变：`python foodManager.py`，但启动后先进入登录页。

---

【6. 旧数据处理】
- 旧 `user_data/records.json` 和 `user_data/profile.json` 不再使用，但为了安全，可在第一次启动时自动将其移动到 `user_data/_old_backup/` 目录下，避免干扰。

---

【7. 交付要求】
请直接给出修改后的完整 `foodManager.py` 代码（或详细 diff 说明），并简要说明启动后的操作流程。
```

---

### 接下来您要做什么？
1. 把上面的 **【任务背景】** 到 **【7. 交付要求】** 全部选中，复制。
2. 打开 **Claude（或 Cursor、Copilot）**，把这段 Prompt 粘贴进去，附上您的 `foodManager.py` 文件。
3. 等待 AI 输出修改后的完整代码，覆盖原文件，然后运行 `python foodManager.py` 即可。

如果 AI 给出的代码有任何小问题（比如依赖缺失、路径报错），您随时可以回来追问，我会帮您继续调试。祝您参赛顺利，登录界面一稿过！ 🚀